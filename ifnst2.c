#include <stdio.h>
int main() {
    int age = 25;
    int experience = 3;

    if (age >= 18) {
        if (experience >= 2) {
            printf("You are eligible for the job.\n");
        }
    }
 
    return 0;
}
